{
  "name": "aegis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "arc:console": "hardhat console --network arcTestnet",
    "arc:deploy:hello": "hardhat run --network arcTestnet scripts/deploy-hello.ts",
    "arc:smoke:hello": "hardhat run --network arcTestnet scripts/smoke-hello.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
